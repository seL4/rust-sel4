[package]
name = "sel4-runtime-building-blocks-unwinding-support"
version = "0.1.0"
authors = ["Nick Spinale <nick.spinale@coliasgroup.com>"]
edition = "2021"
license = "BSD-2-Clause"

[dependencies]
sel4-runtime-building-blocks-elf = { path = "../elf" }
unwinding = { path = "../../../../../more-external/unwinding", default-features = false, features = ["unwinder", "fde-custom", "hide-trace"] }
