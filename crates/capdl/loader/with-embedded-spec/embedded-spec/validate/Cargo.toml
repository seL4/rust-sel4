[package]
name = "capdl-loader-with-embedded-spec-embedded-spec-validate"
version = "0.1.0"
authors = ["Nick Spinale <nick.spinale@coliasgroup.com>"]
edition = "2021"
license = "BSD-2-Clause"

[features]
deflate = ["capdl-loader-with-embedded-spec-embedded-spec/deflate"]

[dependencies]
capdl-loader-with-embedded-spec-build-env = { path = "../../build-env" }
capdl-loader-with-embedded-spec-embedded-spec = { path = ".." }
capdl-types = { path = "../../../../types" }
